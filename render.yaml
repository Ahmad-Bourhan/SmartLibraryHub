services:
  - type: web
    name: SmartLibraryHub
    env: ruby
    buildCommand: "bundle install && bundle exec rails db:migrate"
    startCommand: "bundle exec puma -C config/puma.rb"
    plan: free

